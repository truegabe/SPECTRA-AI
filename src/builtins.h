#ifndef SPECTRA_BUILTINS_H
#define SPECTRA_BUILTINS_H
#include "interpreter.h"

/* Register all built-in functions into the given environment. */
void register_builtins(Env* env);

#endif /* SPECTRA_BUILTINS_H */

#ifndef SPECTRA_REPL_H
#define SPECTRA_REPL_H
#include "interpreter.h"

/* Start the interactive REPL. Returns when the user types 'exit' or 'quit'. */
void repl_run(void);

#endif /* SPECTRA_REPL_H */
